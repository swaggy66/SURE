# SURE

We will release the details coming soon!

## Install

Clone this git repository and change the directory to this repository:

```
pip install -r requirements.txt
```

## Quick Start

The datasets used in our experiments have been uploaded to (https://github.com/butterfliesss/SDT). If you wish to use a different dataset, please ensure that it is processed into a similar format.

```
bash run.sh
```

Once the above commands are executed, the evaluation results for IEMOCAP and MELD will be saved as iemocap.txt/meld.txt.
